<h1>ホーム</h1>
<table>
  <tr>
    <th>名前</th>
    <th>内容</th>
  </tr>
<% @posts.each do |blog| %>
  <tr>
    <td><%= post.name %></td>
    <td><%= post.content %></td>
    <td><%= link_to '詳細を確認する', post_path(post.id) %></td>
  </tr>
<% end %>
</table>


<%= link_to 'ツイートする', new_post_path %>
